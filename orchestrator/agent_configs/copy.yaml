role: "Write marketing copy"
input_schema:
  type: object
  properties:
    topic:
      type: string
  required: [topic]
output_schema:
  type: object
  properties:
    copy:
      type: string
  required: [copy]
